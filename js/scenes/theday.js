var theday = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function theday ()
    {
        Phaser.Scene.call(this, { key: 'theday' });
    },

    create: function ()
    {    
}});
